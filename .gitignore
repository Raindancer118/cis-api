# Build artifacts
cis
explore-cis
explore/

# Local notes (not for the repo)
Project.md

# Reverse-engineering captures — contain session cookies AND personal data.
# NEVER commit these.
Protokoll.txt
*.har
Analyse/

# NOTE: internal/*/testdata/ fixtures are committed but are SYNTHETIC
# (anonymised example data only). Real CIS captures live in Analyse/ (ignored).
